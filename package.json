{
  "name": "webusb-serial",
  "version": "2.0.1",
  "description": "Virtual serial device using WebUSB as the transport",
  "main": "index.js",
  "repository": "git@github.com:monteslu/webusb-serial.git",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Luis Montes",
  "license": "MIT",
  "dependencies": {
  }
}
